import express from 'express'

const router = express.Router()

// change password
// set email
// set useranme if local auth
// set name
// avatar 

// all later because i already learned this

export default router